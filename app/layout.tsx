import "./globals.css";

export const metadata = {
  title: "Legacy Water USA",
  description: "Florida Water Analysis",
  viewport: "width=device-width, initial-scale=1",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}